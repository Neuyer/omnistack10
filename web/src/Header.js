import React from 'react';

function Header(props) {
    return (
        <>
            <header>{props.title}</header>
            <header>{props.title}</header>
            <header>{props.title}</header>
        </>
    );
}
export default Header;